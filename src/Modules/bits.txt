def getBit(int n, int i) = (n & (1 << i)) >> i

def setBit(int n, int i) = n ¦ (1 << i)

def invert(int n) (
	n' = 0
	for i in 0..31 (
		if (getBit(n, i) == 0) (
			n' = setBit(n', i)
		)
	)
	return n'
)

def bitstring(int n) (
	string str = ""
	for i in 31..0 (
		str = str ++ show(getBit(n, i))
	)
	return str
)
