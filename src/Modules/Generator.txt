class Generator (
	def init {func, seed} (
		this.func = func	
		this.seed = seed
		this.first = True
	)

	def next = (
		if first (this.first = False; return seed)
		this.seed = func seed
		return seed
	)

	def toString = "{" ++ show this.seed ++ ".." ++ "}"
)