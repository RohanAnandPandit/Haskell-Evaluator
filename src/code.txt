class Cycle (
	def init {xs} (
		this.list = xs
		this.curr = this.list 
	)
	
	def next = (
		item = head curr
		this.curr = tail curr
		if (curr == []) (
			this.curr = this.list
		)
		return item
	)
	
	def toString = "[" ++ head this.list ++ ".." ++ head this.list ++ "]" 
)
